spring:
  datasource:
    url: jdbc:h2:mem:menusdb;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
show-sql: true
properties:
hibernate:
format_sql: true
server:
port: 8083


menu-service:
jwt:
secret: very_secret_key_that_should_be_changed_in_prod
expiration-ms: 3600000